<nav class="navbar sticky-top navbar-expand-lg navbar-dark" role="navigation">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">OzarkEats</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/restaurants"
            ><i class="fas fa-utensils 7x"></i> Restaurants</a
          >
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/restaurants/new"
            ><i class="fas fa-plus"></i> New</a
          >
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/restaurants/random"
            ><i class="fas fa-random"></i> Random</a
          >
        </li>
      </ul>
      <% if (!currentUser) {%>

      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link" href="/login"
            ><i class="fas fa-sign-in-alt"></i> Login</a
          >
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/register"
            ><i class="fas fa-user-plus"></i> Register</a
          >
        </li>
        <li class="nav-item">
          <form
            action="/restaurants/search"
            class="form-inline d-flex px-3 mt-1 input-group input-group-sm"
            method="GET"
          >
            <input
              type="search"
              name="search"
              id="query"
              placeholder="Restaurant search..."
              class="form-control search"
            />
            <button class="btn btn-light" type="submit">Search</button>
          </form>
        </li>
      </ul>
      <% } else {%>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link active" href="/users/<%= currentUser._id %> "
            ><i class="fas fa-drumstick-bite"></i> <%= currentUser.username %>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/logout"
            ><i class="fas fa-sign-out-alt"></i> Logout</a
          >
        </li>
        <li class="nav-item">
          <form
            action="/restaurants/search"
            class="form-inline d-flex px-3 mt-1 input-group input-group-sm"
            method="GET"
          >
            <input
              type="search"
              name="search"
              id="query"
              placeholder="Restaurant search..."
              class="form-control search"
            />
            <button class="btn btn-light" type="submit">Search</button>
          </form>
        </li>
      </ul>
      <% } %>
    </div>
  </div>
</nav>
